# Team-15-CSK
Adds /premortem custom Claude Code skill that loads the full PreMortem architecture as context. Enables code generation for all 8 system layers, Claude API prompt writing, layer explanations, and build order guidance. Invoke with /premortem &lt;request> — empty invocation lists all capabilities.
